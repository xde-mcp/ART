[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openpipe-art[skypilot]",
]

[tool.uv.sources]
openpipe-art = { path = "../../", editable = true }
