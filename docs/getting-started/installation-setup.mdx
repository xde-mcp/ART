---
title: "Installation + Setup"
sidebarTitle: "Installation + Setup"
icon: "gear"
---

## Install the ART client

The ART client can be installed into projects designed to run on any machine that runs python.

```bash
pip install openpipe-art
```
